const keywords = {

"ミクロ経済学": {
  definition: "個々の家計や企業の行動を分析する経済学の分野。",
  example: "需要と供給の理論など。",
  related: "マクロ経済学、需要、供給"
},

"マクロ経済学": {
  definition: "経済全体の動きを分析する経済学の分野。",
  example: "GDPやインフレ率の分析。",
  related: "GDP、CPI、失業率"
},

"GDP": {
  definition: "国内総生産。一定期間に国内で生産された財・サービスの総価値。",
  example: "経済規模の代表的指標。",
  related: "成長率、マクロ経済"
},

"GNP": {
  definition: "国民総生産。国民が国内外で生産した付加価値の合計。",
  example: "海外収益も含む指標。",
  related: "GDP"
},

"CPI": {
  definition: "消費者物価指数。物価水準の変化を示す指標。",
  example: "インフレ率の計算に使われる。",
  related: "インフレ"
},

"インフレ": {
  definition: "物価が継続的に上昇する現象。",
  example: "パンの価格が上がる。",
  related: "CPI、デフレ"
},

"デフレ": {
  definition: "物価が継続的に下落する現象。",
  example: "景気低迷期に起こりやすい。",
  related: "インフレ"
},

"失業率": {
  definition: "労働力人口のうち失業者の割合。",
  example: "景気判断指標。",
  related: "雇用"
},

"需要": {
  definition: "消費者が購入したい財の量。",
  example: "価格が下がると増える。",
  related: "供給"
},

"供給": {
  definition: "生産者が市場に提供する財の量。",
  example: "価格が上がると増える。",
  related: "需要"
},

"市場": {
  definition: "財やサービスが取引される場。",
  example: "完全競争市場など。",
  related: "価格"
},

"価格": {
  definition: "財やサービスの交換価値。",
  example: "市場で決定される。",
  related: "需要、供給"
},

"弾力性": {
  definition: "価格や所得の変化に対する需要の反応度。",
  example: "価格弾力性など。",
  related: "需要"
},

"完全競争": {
  definition: "多数の売り手と買い手が存在する市場構造。",
  example: "価格は市場で決まる。",
  related: "市場"
},

"独占": {
  definition: "一つの企業が市場を支配する状態。",
  example: "電力会社など。",
  related: "市場構造"
},

"GDP成長率": {
  definition: "GDPの増加率。",
  example: "経済成長の指標。",
  related: "GDP"
},

"金融政策": {
  definition: "中央銀行が行う通貨・金利の調整政策。",
  example: "利上げ・利下げ。",
  related: "金利"
},

"財政政策": {
  definition: "政府による支出や税の調整政策。",
  example: "減税や公共投資。",
  related: "政府"
},

"金利": {
  definition: "資金の貸借に対する利子の割合。",
  example: "中央銀行が操作する。",
  related: "金融政策"
},

"為替": {
  definition: "異なる通貨の交換比率。",
  example: "円安・円高。",
  related: "外貨"
},

"円安": {
  definition: "円の価値が下がる状態。",
  example: "輸入価格が上昇。",
  related: "為替"
},

"円高": {
  definition: "円の価値が上がる状態。",
  example: "輸入が安くなる。",
  related: "為替"
},

"貿易": {
  definition: "国と国の間の商品・サービスの交換。",
  example: "輸出・輸入。",
  related: "国際経済"
},

"輸出": {
  definition: "国内から国外へ商品を売ること。",
  example: "自動車輸出。",
  related: "貿易"
},

"輸入": {
  definition: "国外から商品を買うこと。",
  example: "エネルギー輸入。",
  related: "貿易"
},

"インフラ": {
  definition: "経済活動の基盤となる社会資本。",
  example: "道路・電力・通信。",
  related: "公共投資"
},

"労働市場": {
  definition: "労働力の需要と供給が形成される市場。",
  example: "雇用状況。",
  related: "失業率"
},

"賃金": {
  definition: "労働の対価として支払われる報酬。",
  example: "最低賃金など。",
  related: "労働"
},

"最低賃金": {
  definition: "法律で定められた最低の賃金水準。",
  example: "政府が設定。",
  related: "賃金"
},

"インセンティブ": {
  definition: "人々の行動を変える動機づけ。",
  example: "ボーナス制度など。",
  related: "行動経済学"
},

"機会費用": {
  definition: "ある選択をしたときに失われる最善の代替案の価値。",
  example: "時間の使い方。",
  related: "意思決定"
},

"効用": {
  definition: "消費者が財から得る満足度。",
  example: "選択理論で使用。",
  related: "消費"
},

"限界効用": {
  definition: "追加的に得られる効用の増加分。",
  example: "経済学の基本概念。",
  related: "効用"
},

"生産性": {
  definition: "投入に対する産出の効率。",
  example: "労働生産性など。",
  related: "経済成長"
},

"投資": {
  definition: "将来の利益のための資本支出。",
  example: "設備投資。",
  related: "資本"
},

"貯蓄": {
  definition: "所得のうち消費されない部分。",
  example: "銀行預金など。",
  related: "所得"
},

"所得": {
  definition: "個人や企業が得る収入。",
  example: "給与収入など。",
  related: "賃金"
},

"通貨": {
  definition: "交換手段としての貨幣。",
  example: "円、ドル。",
  related: "金融"
},

"貨幣": {
  definition: "交換や価値保存の手段。",
  example: "現金・預金。",
  related: "通貨"
},

"中央銀行": {
  definition: "国家の金融政策を担当する機関。",
  example: "日本銀行。",
  related: "金融政策"
},

"日本銀行": {
  definition: "日本の中央銀行。",
  example: "金融政策を実施。",
  related: "中央銀行"
},

"インフレ期待": {
  definition: "将来の物価上昇に対する予想。",
  example: "賃金交渉に影響。",
  related: "インフレ"
},

"景気": {
  definition: "経済活動の全体的な状態。",
  example: "景気拡大・後退。",
  related: "GDP"
},

"景気循環": {
  definition: "経済が拡大と縮小を繰り返す現象。",
  example: "ビジネスサイクル。",
  related: "景気"
},

"公共財": {
  definition: "非排除性と非競合性を持つ財。",
  example: "国防・治安。",
  related: "政府"
},

"外部性": {
  definition: "市場取引以外で第三者に影響を与える効果。",
  example: "環境汚染など。",
  related: "市場失敗"
},

"市場失敗": {
  definition: "市場が効率的に資源配分できない状態。",
  example: "独占・外部性。",
  related: "政府介入"
}

};

const FAVORITE_KEY = "fav";
const HISTORY_KEY = "searchHistory";
const MAX_HISTORY_COUNT = 10;

function getStorageList(key){
    try{
        return JSON.parse(localStorage.getItem(key)) || [];
    }catch(e){
        return [];
    }
}

function setStorageList(key, list){
    localStorage.setItem(key, JSON.stringify(list));
}

function escapeHTML(text){
    return String(text).replace(/[&<>'"]/g, function(char){
        return {
            "&": "&amp;",
            "<": "&lt;",
            ">": "&gt;",
            "'": "&#039;",
            '"': "&quot;"
        }[char];
    });
}

function findKeyword(input){
    const word = String(input || "")
        .trim()
        .replace(/\s+/g, "")
        .toLowerCase();

    const keys = Object.keys(keywords);

    // 完全匹配
    for(const key of keys){
        if(key.toLowerCase() === word){
            return key;
        }
    }

    // 包含匹配
    for(const key of keys){
        if(key.toLowerCase().includes(word)){
            return key;
        }
    }

    // 反向包含
    for(const key of keys){
        if(word.includes(key.toLowerCase())){
            return key;
        }
    }

    return null;
}

function createNewsSearchUrl(word, title){
    const query = encodeURIComponent(`${word} ${title}`);
    return `https://news.google.com/search?q=${query}&hl=ja&gl=JP&ceid=JP:ja`;
}

function renderNewsCases(word){
    const cases = newsCases[word] || [];

    if(cases.length === 0){
        return `
            <div class="news-section">
                <div class="section-title">📰 ニュース事例</div>
                <p class="empty-text">このキーワードのニュース事例はまだ登録されていません。</p>
            </div>
        `;
    }

    return `
        <div class="news-section">
            <div class="section-title">📰 ニュース事例</div>
            <p class="news-lead">このキーワードが実際のニュースでどのように使われるかを確認できます。</p>

            <div class="news-grid">
                ${cases.map(item => `
                    <div class="news-card">
                        <h3>${escapeHTML(item.title)}</h3>
                        <p>${escapeHTML(item.summary)}</p>
                        <div class="news-point">学習ポイント：${escapeHTML(item.point)}</div>
                        <a class="news-link" href="${createNewsSearchUrl(word, item.title)}" target="_blank" rel="noopener noreferrer">
                            関連ニュースを探す
                        </a>
                    </div>
                `).join("")}
            </div>

            <a class="latest-news-link" href="${createNewsSearchUrl(word, "")}" target="_blank" rel="noopener noreferrer">
                「${escapeHTML(word)}」の最新ニュースをまとめて見る
            </a>
        </div>
    `;
}

function searchWord(wordInput){
    const input = document.getElementById("searchInput");
    const rawWord = wordInput || input.value;
    const word = findKeyword(rawWord);
    const data = keywords[word];
    const resultBox = document.getElementById("resultBox");

    if(!String(rawWord).trim()){
        resultBox.innerHTML = `<p class="notfound">キーワードを入力してください</p>`;
        return;
    }

    input.value = word;
    addHistory(word);

    if(!data){
        resultBox.innerHTML = `<p class="notfound">「${escapeHTML(word)}」は見つかりませんでした</p>`;
        return;
    }

    const favoriteList = getStorageList(FAVORITE_KEY);
    const isFavorite = favoriteList.includes(word);

    resultBox.innerHTML = `
        <h2 class="keyword-title">${escapeHTML(word)}</h2>

        <div class="section-title">📖 定義</div>
        <p>${escapeHTML(data.definition)}</p>

        <div class="section-title">💡 例</div>
        <p>${escapeHTML(data.example)}</p>

        <div class="section-title">🔗 関連</div>
        <div>
            ${data.related.split("、").map(r =>
                `<span class="related-tag">${escapeHTML(r)}</span>`
            ).join("")}
        </div>

        ${renderNewsCases(word)}

        <button class="favorite-btn" onclick="toggleFavorite('${escapeHTML(word)}')">
            ${isFavorite ? "★ お気に入り解除" : "⭐ お気に入りに追加"}
        </button>
    `;
}

function quickSearch(word){
    document.getElementById("searchInput").value = word;
    searchWord(word);
}

function addFavorite(word){
    let list = getStorageList(FAVORITE_KEY);

    if(!list.includes(word)){
        list.push(word);
    }

    setStorageList(FAVORITE_KEY, list);
    renderFav();
}

function removeFavorite(word){
    let list = getStorageList(FAVORITE_KEY);
    list = list.filter(item => item !== word);

    setStorageList(FAVORITE_KEY, list);
    renderFav();
}

function toggleFavorite(word){
    let list = getStorageList(FAVORITE_KEY);

    if(list.includes(word)){
        list = list.filter(item => item !== word);
    }else{
        list.push(word);
    }

    setStorageList(FAVORITE_KEY, list);
    renderFav();
    searchWord(word);
}

function renderFav(){
    const list = getStorageList(FAVORITE_KEY);
    const favBox = document.getElementById("favorites");

    if(!favBox){
        return;
    }

    if(list.length === 0){
        favBox.innerHTML = `<li class="empty-text">まだお気に入りはありません</li>`;
        return;
    }

    favBox.innerHTML = list.map(word => `
        <li class="saved-item">
            <span onclick="searchWord('${escapeHTML(word)}')">${escapeHTML(word)}</span>
            <button class="small-btn danger-btn" onclick="removeFavorite('${escapeHTML(word)}')">削除</button>
        </li>
    `).join("");
}

function addHistory(word){
    let history = getStorageList(HISTORY_KEY);

    history = history.filter(item => item.word !== word);
    history.unshift({
        word: word,
        time: new Date().toLocaleString("ja-JP")
    });
    history = history.slice(0, MAX_HISTORY_COUNT);

    setStorageList(HISTORY_KEY, history);
    renderHistory();
}

function renderHistory(){
    const historyBox = document.getElementById("historyList");
    const history = getStorageList(HISTORY_KEY);

    if(!historyBox){
        return;
    }

    if(history.length === 0){
        historyBox.innerHTML = `<li class="empty-text">まだ検索履歴はありません</li>`;
        return;
    }

    historyBox.innerHTML = history.map(item => `
        <li class="saved-item">
            <span onclick="searchWord('${escapeHTML(item.word)}')">${escapeHTML(item.word)}</span>
            <small>${escapeHTML(item.time)}</small>
        </li>
    `).join("");
}

function clearHistory(){
    localStorage.removeItem(HISTORY_KEY);
    renderHistory();
}

const searchInput = document.getElementById("searchInput");
searchInput.addEventListener("keydown", function(event){
    if(event.key === "Enter"){
        searchWord();
    }
});

renderFav();
renderHistory();
